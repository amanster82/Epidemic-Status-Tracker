import React from 'react';
import UserInfo from './components/SimpleCard'
//import Grid from '@material-ui/core/Grid'

function Profile(props) {
    return (
        <div>
            <UserInfo></UserInfo>
        </div>
    );
}

export default Profile;